<div class="card">
   <h3 class="card-header">
      <a href="/post/{{id}}">{{title}}</a> 
      <p>Post by {{user.name}} on {{format_date date_created}}</p>
   </h3>
   
  <div class="card-body">
    <p>
      {{content}}
    </p>
  </div>
</div>